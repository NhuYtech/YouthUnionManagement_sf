<form method="post" action="{{ path('app_instruction_document_delete', {'id': instruction_document.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ instruction_document.id) }}">
    <button class="btn">Xóa</button>
</form>
