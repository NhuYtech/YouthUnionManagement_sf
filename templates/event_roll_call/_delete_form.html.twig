<form method="post" action="{{ path('app_event_roll_call_delete', {'id': event_roll_call.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">

    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ event_roll_call.id) }}">
    <button class="btn btn-danger">
        <i class="fas fa-trash-alt"></i> Xóa
    </button>
</form>