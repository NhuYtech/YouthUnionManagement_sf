<!-- Topbar -->
<nav class="navbar navbar-expand navbar-light topbar mb-4 static-top shadow" style="background-color: #08326b; color: white;">

    <!-- Topbar Navbar -->
    <ul class="navbar-nav ml-auto">

        <!-- Thông tin người dùng -->
        <li class="nav-item dropdown no-arrow">
            <a class="nav-link dropdown-toggle" href="#" id="userDropdown" role="button" data-toggle="dropdown">
                <div class="sidebar-brand-icon">
                    <img src="{{ asset('admin/img/avt.png') }}"
                         alt="User Avatar"
                         class="rounded-circle"
                         width="35"
                         height="35"
                         style="object-fit: cover;">
                </div>
                <span class="mr-2 d-none d-lg-inline font-weight-bold text-white" style="font-size: 16px;">
                    {{ app.user.fullName|default('Người Dùng') }}
                </span>
            </a>
            <div class="dropdown-menu dropdown-menu-right shadow animated--grow-in" aria-labelledby="userDropdown">

                {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_USER') and not is_granted('ROLE_SECRETARY') %}
                <a class="dropdown-item" href="{{ path('app_profile') }}">
                    <i class="fas fa-user fa-sm fa-fw mr-2 text-gray-400"></i>
                    Hồ sơ
                </a>
                {% endif %}
                
                <a class="dropdown-item" href="{{ path('app_change_password') }}">
                    <i class="fas fa-key fa-sm fa-fw mr-2 text-gray-400"></i>
                    Đổi mật khẩu
                </a>

                {% if app.user is defined and app.user is not null %}
                <a class="dropdown-item" href="{{ path('app_logout') }}">
                    <i class="fas fa-sign-out-alt fa-sm fa-fw mr-2 text-gray-400"></i>
                    Đăng xuất
                </a>
                {% endif %}
            </div>
        </li>
    </ul>
</nav>
<!-- End of Topbar -->
