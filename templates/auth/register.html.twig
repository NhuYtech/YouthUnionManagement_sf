<!DOCTYPE html>
<html lang="en">

<head>

    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="description" content="">
    <meta name="author" content="">

    <title>Tạo tài khoản</title>

    <!-- Favicon -->
    <link rel="icon" type="image/png" href="{{ asset('admin/img/logo.png') }}">

    <!-- Custom fonts for this template-->
    <link href="{{ asset('admin/vendor/fontawesome-free/css/all.min.css') }}" rel="stylesheet">
    
    <link
        href="https://fonts.googleapis.com/css?family=Nunito:200,200i,300,300i,400,400i,600,600i,700,700i,800,800i,900,900i"
        rel="stylesheet">

        <!-- Custom styles for this template-->
        <link href="{{ asset('admin/css/sb-admin-2.min.css') }}" rel="stylesheet">

</head>

<body style="background: url('{{ asset('admin/img/nen.png') }}') no-repeat center center fixed; background-size: cover;">

    <div class="container">
    <div class="row justify-content-center align-items-center" style="min-height: 100vh;">
        <!-- Form đăng ký căn giữa -->
        <div class="col-md-6 col-lg-5 d-flex align-items-center">
            <div class="card o-hidden border-0 shadow-lg p-4 w-100">
                <div class="text-center">
                    <img src="{{ asset('admin/img/yumm.png') }}" alt="Logo Đoàn" style="max-width: 220px; height: auto; margin-bottom: 20px;">
                </div>
                <div class="text-center">
                    <h1 class="h6" style="font-weight: bold; color: black; margin-bottom: 1rem;">ĐĂNG KÝ</h1>
                </div>

                <!-- Flash messages -->
                {% for flash in app.flashes('error') %}
                    <div class="alert alert-danger text-center">{{ flash }}</div>
                {% endfor %}
                {% for flash in app.flashes('success') %}
                    <div class="alert alert-success text-center">{{ flash }}</div>
                {% endfor %}

                <form class="user" method="post">
                    {{ form_start(form, {'attr': {'class': 'user'}}) }}

                    <!-- Họ và tên -->
                    <div class="form-group">
                        {{ form_widget(form.fullName, {'attr': {'class': 'form-control form-control-user mb-3', 'placeholder': 'Vui lòng nhập họ và tên'}}) }}
                        <div class="text-danger">{{ form_errors(form.fullName) }}</div>
                        <small class="form-text text-note"><strong>*</strong> Họ và tên không được để trống, ít nhất 3 ký tự.</small>
                    </div>

                    <!-- Email -->
                    <div class="form-group">
                        {{ form_widget(form.email, {'attr': {'class': 'form-control form-control-user mb-3', 'placeholder': 'Vui lòng điền email'}}) }}
                        <div class="text-danger">{{ form_errors(form.email) }}</div>
                        <small class="form-text text-note"><strong>*</strong> Nhập email hợp lệ, ví dụ: example@example.com.</small>
                    </div>

                    <!-- Số điện thoại -->
                    <div class="form-group">
                        {{ form_widget(form.phoneNumber, {'attr': {'class': 'form-control form-control-user mb-3', 'placeholder': 'Vui lòng điền số điện thoại'}}) }}
                        <div class="text-danger">{{ form_errors(form.phoneNumber) }}</div>
                        <small class="form-text text-note"><strong>*</strong> Số điện thoại phải bắt đầu bằng 0 và có 10 chữ số.</small>
                    </div>

                    <!-- Mật khẩu -->
                    <div class="form-group">
                        {{ form_widget(form.password.first, {'attr': {'class': 'form-control form-control-user mb-3', 'placeholder': 'Mật khẩu'}}) }}
                        <div class="text-danger">{{ form_errors(form.password.first) }}</div>
                        <small class="form-text text-note"><strong>*</strong> Mật khẩu phải có ít nhất 6 ký tự, bao gồm chữ hoa, chữ thường, số và ký tự đặc biệt.</small>
                    </div>

                    <!-- Xác nhận mật khẩu -->
                    <div class="form-group">
                        {{ form_widget(form.password.second, {'attr': {'class': 'form-control form-control-user mb-3', 'placeholder': 'Nhập lại mật khẩu'}}) }}
                        <div class="text-danger">{{ form_errors(form.password.second) }}</div>
                        <small class="form-text text-note"><strong>*</strong> Mật khẩu xác nhận phải trùng khớp với mật khẩu trên.</small>
                    </div>

                    {{ form_end(form) }}
                <hr>
                </form>

                <div class="text-center">
                    <a href="{{ path('app_forgot_password') }}">Quên mật khẩu?</a>
                </div>
                <div class="text-center">
                    <a href="{{ path('app_login') }}">Bạn đã có tài khoản? Đăng nhập!</a>
                </div>
            </div>
        </div>
    </div>
</div>

                
    <!-- Bootstrap core JavaScript-->
    <script src="{{ asset('admin/vendor/jquery/jquery.min.js') }}"></script>
    <script src="{{ asset('admin/vendor/bootstrap/js/bootstrap.bundle.min.js') }}"></script>

    <!-- Core plugin JavaScript-->
    <script src="{{ asset('admin/vendor/jquery-easing/jquery.easing.min.js') }}"></script>

    <!-- Custom scripts for all pages-->
    <script src="{{ asset('admin/js/sb-admin-2.min.js') }}"></script>

</body>
</html>